/**
 * Primitive Types
 * string
 * number
 * boolean
 * null
 * undefined
 * 
 */


const course: string = "TypeScript";  // Explioit variable
const myName = "sujon";  // Implioit variable
const code: number = 20002;
const ActiveKey: boolean = true;


/**
 * Array and Tuples
 */

const number: number[] = [1, 2, 3, 4];
console.log(number);

const names: string[] = ['sujon', 'shakib', 'sumon'];
console.log(names);

// tupple
const tuplestest: [number, string] = [101, "sujon"];
console.log(tuplestest[1]);
